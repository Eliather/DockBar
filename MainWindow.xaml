<Window x:Class="DockBar.MainWindow"
        x:Name="DockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:DockBar.Converters"
        xmlns:local="clr-namespace:DockBar"
        Title="DockBar"
        Width="320"
        Height="600"
        Topmost="True"
        WindowStyle="None"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        RenderOptions.BitmapScalingMode="HighQuality"
        Background="Transparent"
        AllowsTransparency="False"
        Loaded="Window_Loaded"
        Activated="Window_Activated"
        MouseEnter="Window_MouseEnter"
        MouseLeave="Window_MouseLeave"
        MouseMove="Window_MouseMove"
        AllowDrop="True"
        Drop="Window_Drop">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <conv:InverseBoolConverter x:Key="InverseBoolConverter"/>
        <conv:IntEqualsMultiToVisibilityConverter x:Key="IntEqualsMultiToVisibilityConverter"/>
        <conv:StringEqualsMultiToBoolConverter x:Key="StringEqualsMultiToBoolConverter"/>

        <Style x:Key="CharmItemButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="root"
                                Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="root" Property="Background" Value="#22FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="root" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BottomIconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{Binding DockTextBrush, RelativeSource={RelativeSource AncestorType=Window}}"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="ToolTipService.InitialShowDelay" Value="150"/>
            <Setter Property="ToolTipService.ShowDuration" Value="2400"/>
            <Setter Property="ToolTipService.BetweenShowDelay" Value="80"/>
            <Setter Property="ToolTipService.Placement" Value="Top"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="root" Property="Background" Value="#22FFFFFF"/>
                                <Setter TargetName="root" Property="BorderBrush" Value="#6E8BC4"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="root" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DockActionStrokeIconStyle" TargetType="Path">
            <Setter Property="Stroke" Value="{Binding DockTextBrush, RelativeSource={RelativeSource AncestorType=Window}}"/>
            <Setter Property="StrokeThickness" Value="1.8"/>
            <Setter Property="StrokeStartLineCap" Value="Round"/>
            <Setter Property="StrokeEndLineCap" Value="Round"/>
            <Setter Property="StrokeLineJoin" Value="Round"/>
            <Setter Property="Stretch" Value="Uniform"/>
        </Style>
        <Style x:Key="DockToolTipStyle" TargetType="ToolTip">
            <Setter Property="Background" Value="{DynamicResource AppSurfaceRaisedBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AppTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AppBorderStrongBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="HasDropShadow" Value="True"/>
        </Style>
    </Window.Resources>

    <Border Background="{Binding DockBackgroundBrush, RelativeSource={RelativeSource AncestorType=Window}}" Padding="10">
        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ItemsControl x:Name="NormalList"
                          Grid.Row="0"
                          ItemsSource="{Binding VisibleShortcuts}"
                          HorizontalAlignment="Center"
                          Visibility="{Binding IsEditMode, Converter={StaticResource InverseBoolConverter}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="6" Background="Transparent">
                            <Button Style="{StaticResource CharmItemButtonStyle}"
                                    Click="Shortcut_Click"
                                    Tag="{Binding Path}">
                                <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                                    <Image Width="{Binding IconSize, RelativeSource={RelativeSource AncestorType=Window}}"
                                           Height="{Binding IconSize, RelativeSource={RelativeSource AncestorType=Window}}"
                                           Source="{Binding Icon}"
                                           VerticalAlignment="Center"
                                           Stretch="Uniform"
                                           RenderOptions.BitmapScalingMode="HighQuality"
                                           SnapsToDevicePixels="True">
                                        <Image.Effect>
                                            <DropShadowEffect Color="#66000000"
                                                              BlurRadius="10"
                                                              ShadowDepth="0"
                                                              Opacity="0.8"/>
                                        </Image.Effect>
                                    </Image>
                                    <TextBlock Text="{Binding Name}"
                                               Margin="0,8,0,0"
                                               Foreground="{Binding DockTextBrush, RelativeSource={RelativeSource AncestorType=Window}}"
                                               TextAlignment="Center"
                                               TextWrapping="Wrap"
                                               FontSize="12"
                                               Opacity="0.9"/>
                                </StackPanel>
                            </Button>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <ScrollViewer x:Name="EditListScroll"
                          Grid.Row="0"
                          Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVisibilityConverter}}"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding Shortcuts}"
                              AlternationCount="1000"
                              AllowDrop="True"
                              PreviewMouseLeftButtonDown="ShortcutItem_PreviewMouseLeftButtonDown"
                              PreviewMouseMove="ShortcutList_PreviewMouseMove"
                              Drop="ShortcutList_Drop"
                              DragOver="ShortcutList_DragOver">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="6" Background="Transparent" x:Name="ItemBorder" BorderBrush="Transparent" BorderThickness="1">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="BorderBrush" Value="Transparent"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger>
                                                <DataTrigger.Binding>
                                                    <MultiBinding Converter="{StaticResource StringEqualsMultiToBoolConverter}">
                                                        <Binding Path="Path"/>
                                                        <Binding RelativeSource="{RelativeSource AncestorType=Window}" Path="DragHoverPath"/>
                                                    </MultiBinding>
                                                </DataTrigger.Binding>
                                                <Setter Property="BorderBrush" Value="{Binding DataContext.DockTextBrush, RelativeSource={RelativeSource AncestorType=Window}}"/>
                                                <Setter Property="BorderThickness" Value="1.5"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Rectangle Height="1.5"
                                               StrokeDashArray="2 2"
                                               Stroke="{Binding DataContext.DockTextBrush, RelativeSource={RelativeSource AncestorType=Window}}"
                                               VerticalAlignment="Top">
                                        <Rectangle.Visibility>
                                            <MultiBinding Converter="{StaticResource IntEqualsMultiToVisibilityConverter}">
                                                <Binding RelativeSource="{RelativeSource AncestorType=ContentPresenter}" Path="(ItemsControl.AlternationIndex)"/>
                                                <Binding RelativeSource="{RelativeSource AncestorType=Window}" Path="DropInsertIndex"/>
                                            </MultiBinding>
                                        </Rectangle.Visibility>
                                    </Rectangle>
                                    <Button Style="{StaticResource CharmItemButtonStyle}"
                                            Click="Shortcut_Click"
                                            Tag="{Binding Path}"
                                            IsHitTestVisible="{Binding DataContext.IsEditMode, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource InverseBoolConverter}}">
                                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                                            <Image Width="{Binding IconSize, RelativeSource={RelativeSource AncestorType=Window}}"
                                                   Height="{Binding IconSize, RelativeSource={RelativeSource AncestorType=Window}}"
                                                   Source="{Binding Icon}"
                                                   VerticalAlignment="Center"
                                                   Stretch="Uniform"
                                                   RenderOptions.BitmapScalingMode="HighQuality"
                                                   SnapsToDevicePixels="True">
                                                <Image.Effect>
                                                    <DropShadowEffect Color="#66000000"
                                                                      BlurRadius="10"
                                                                      ShadowDepth="0"
                                                                      Opacity="0.8"/>
                                                </Image.Effect>
                                            </Image>
                                            <TextBlock Text="{Binding Name}"
                                                       Margin="0,8,0,0"
                                                       Foreground="{Binding DockTextBrush, RelativeSource={RelativeSource AncestorType=Window}}"
                                                       TextAlignment="Center"
                                                       TextWrapping="Wrap"
                                                       FontSize="12"
                                                       Opacity="0.9"/>
                                        </StackPanel>
                                    </Button>
                                    <StackPanel Grid.Column="1"
                                                Orientation="Horizontal"
                                                VerticalAlignment="Center"
                                                Visibility="{Binding DataContext.IsEditMode, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <Button Style="{StaticResource BottomIconButtonStyle}"
                                                Width="28"
                                                Height="28"
                                                Margin="4,0,0,0"
                                                Click="Rename_Click"
                                                Tag="{Binding Path}">
                                            <Button.ToolTip>
                                                <ToolTip Style="{StaticResource DockToolTipStyle}"
                                                         Content="{local:Loc Key=Dock_TooltipRename}"/>
                                            </Button.ToolTip>
                                            <Viewbox Width="13" Height="13">
                                                <Grid Width="24" Height="24">
                                                    <Path Style="{StaticResource DockActionStrokeIconStyle}" Data="M6.5,17.5L9.75,16.75L17.75,8.75L15.25,6.25L7.25,14.25Z"/>
                                                    <Path Style="{StaticResource DockActionStrokeIconStyle}" Data="M14,7.5L16.5,10"/>
                                                </Grid>
                                            </Viewbox>
                                        </Button>
                                        <Button Style="{StaticResource BottomIconButtonStyle}"
                                                Width="28"
                                                Height="28"
                                                Margin="4,0,0,0"
                                                Click="ChangeIcon_Click"
                                                Tag="{Binding Path}">
                                            <Button.ToolTip>
                                                <ToolTip Style="{StaticResource DockToolTipStyle}"
                                                         Content="{local:Loc Key=Dock_TooltipChangeIcon}"/>
                                            </Button.ToolTip>
                                            <Viewbox Width="13" Height="13">
                                                <Grid Width="24" Height="24">
                                                    <Path Style="{StaticResource DockActionStrokeIconStyle}" Data="M5.5,6.75H18.5C19.05,6.75 19.5,7.2 19.5,7.75V16.25C19.5,16.8 19.05,17.25 18.5,17.25H5.5C4.95,17.25 4.5,16.8 4.5,16.25V7.75C4.5,7.2 4.95,6.75 5.5,6.75Z"/>
                                                    <Path Style="{StaticResource DockActionStrokeIconStyle}" Data="M6.75,15L10.5,11.25L13,13.75L16.25,10.5L18,12.25"/>
                                                    <Ellipse Width="3"
                                                             Height="3"
                                                             Stroke="{Binding DockTextBrush, RelativeSource={RelativeSource AncestorType=Window}}"
                                                             StrokeThickness="1.4"
                                                             Fill="Transparent"
                                                             HorizontalAlignment="Right"
                                                             VerticalAlignment="Top"
                                                             Margin="0,0,8,6"/>
                                                </Grid>
                                            </Viewbox>
                                        </Button>
                                        <Button Style="{StaticResource BottomIconButtonStyle}"
                                                Width="28"
                                                Height="28"
                                                Margin="4,0,0,0"
                                                Click="Remove_Click"
                                                Tag="{Binding Path}">
                                            <Button.ToolTip>
                                                <ToolTip Style="{StaticResource DockToolTipStyle}"
                                                         Content="{local:Loc Key=Dock_TooltipRemove}"/>
                                            </Button.ToolTip>
                                            <Viewbox Width="13" Height="13">
                                                <Grid Width="24" Height="24">
                                                    <Path Style="{StaticResource DockActionStrokeIconStyle}" Data="M9,7V5.75C9,5.06 9.56,4.5 10.25,4.5H13.75C14.44,4.5 15,5.06 15,5.75V7"/>
                                                    <Path Style="{StaticResource DockActionStrokeIconStyle}" Data="M5.5,7H18.5"/>
                                                    <Path Style="{StaticResource DockActionStrokeIconStyle}" Data="M7.75,8.5L8.5,18H15.5L16.25,8.5"/>
                                                    <Path Style="{StaticResource DockActionStrokeIconStyle}" Data="M10,10.5V15 M14,10.5V15"/>
                                                </Grid>
                                            </Viewbox>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <StackPanel x:Name="PaginationPanel"
                        Grid.Row="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,8,0,4"
                        Visibility="{Binding PaginationVisibility}">
                <Button Style="{StaticResource BottomIconButtonStyle}"
                        Width="28"
                        Height="28"
                        Click="PrevPage_Click">
                    <Viewbox Width="14" Height="14">
                        <Grid Width="24" Height="24">
                            <Path Style="{StaticResource DockActionStrokeIconStyle}" Data="M14.5,5L8.5,12L14.5,19"/>
                        </Grid>
                    </Viewbox>
                </Button>
                <TextBlock Text="{Binding PageInfo}"
                           Margin="8,0,8,0"
                           VerticalAlignment="Center"
                           Foreground="{Binding DockTextBrush, RelativeSource={RelativeSource AncestorType=Window}}"/>
                <Button Style="{StaticResource BottomIconButtonStyle}"
                        Width="28"
                        Height="28"
                        Click="NextPage_Click">
                    <Viewbox Width="14" Height="14">
                        <Grid Width="24" Height="24">
                            <Path Style="{StaticResource DockActionStrokeIconStyle}" Data="M9.5,5L15.5,12L9.5,19"/>
                        </Grid>
                    </Viewbox>
                </Button>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,10">
                <Button Style="{StaticResource BottomIconButtonStyle}"
                        x:Name="AddButton"
                        Click="AddShortcutMenu_Click">
                    <Button.ToolTip>
                        <ToolTip Style="{StaticResource DockToolTipStyle}"
                                 Content="{local:Loc Key=Dock_TooltipAdd}"/>
                    </Button.ToolTip>
                    <Viewbox Width="16" Height="16">
                        <Grid Width="24" Height="24">
                            <Path Style="{StaticResource DockActionStrokeIconStyle}" Data="M12,7V17 M7,12H17"/>
                        </Grid>
                    </Viewbox>
                </Button>
                <Button Style="{StaticResource BottomIconButtonStyle}"
                        Margin="8,0,0,0"
                        Click="Settings_Click">
                    <Button.ToolTip>
                        <ToolTip Style="{StaticResource DockToolTipStyle}"
                                 Content="{local:Loc Key=Dock_TooltipSettings}"/>
                    </Button.ToolTip>
                    <Viewbox Width="16" Height="16">
                        <Grid Width="24" Height="24">
                            <Path Style="{StaticResource DockActionStrokeIconStyle}" Data="M6.5,8H17.5 M6.5,12H17.5 M6.5,16H17.5"/>
                        </Grid>
                    </Viewbox>
                </Button>
                <Button Style="{StaticResource BottomIconButtonStyle}"
                        Margin="8,0,0,0"
                        Click="ToggleEdit_Click">
                    <Button.ToolTip>
                        <ToolTip Style="{StaticResource DockToolTipStyle}"
                                 Content="{local:Loc Key=Dock_TooltipEditMode}"/>
                    </Button.ToolTip>
                    <Viewbox Width="16" Height="16">
                        <Grid Width="24" Height="24">
                            <Path Style="{StaticResource DockActionStrokeIconStyle}" Data="M6.5,17.5L9.75,16.75L17.75,8.75L15.25,6.25L7.25,14.25Z"/>
                            <Path Style="{StaticResource DockActionStrokeIconStyle}" Data="M13.75,7.75L16.25,10.25"/>
                        </Grid>
                    </Viewbox>
                </Button>
                <Button Style="{StaticResource BottomIconButtonStyle}"
                        Margin="8,0,0,0"
                        Click="Close_Click">
                    <Button.ToolTip>
                        <ToolTip Style="{StaticResource DockToolTipStyle}"
                                 Content="{local:Loc Key=Dock_TooltipClose}"/>
                    </Button.ToolTip>
                    <Viewbox Width="16" Height="16">
                        <Grid Width="24" Height="24">
                            <Path Style="{StaticResource DockActionStrokeIconStyle}" Data="M8,8L16,16 M16,8L8,16"/>
                        </Grid>
                    </Viewbox>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
